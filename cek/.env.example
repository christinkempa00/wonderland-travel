# Kosongkan untuk development (Vite dev server sudah proxy /api ke localhost:4000).
# Untuk build production, isi dengan URL backend yang sudah di-deploy, tanpa trailing slash:
# VITE_API_BASE_URL="https://admin.wonderlandtravel.com"
VITE_API_BASE_URL=
